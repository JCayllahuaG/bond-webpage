---
import '../../styles/global.css';
import Layout from '../../layouts/Layout.astro';
import VideoCarousel from '../../components/VideoCarousel.astro';
import HeroNavbar from '../../components/HeroNavbar.astro';
import LookSection from '../../components/LookSection.astro';
import SiteFooter from '../../components/SiteFooter.astro';
import { assetUrl } from '../../lib/assets';
import { siteCopy } from '../../lib/i18n';

const lang = 'en';
const copy = siteCopy[lang];

const heroVideos = [
  { src: assetUrl('/videos/video_3.mp4') },
  { src: assetUrl('/videos/video_1.mp4') },
  { src: assetUrl('/videos/video_2.mp4') },
  { src: assetUrl('/videos/video_4.mp4') },
];
---

<Layout
  lang={lang}
  title={copy.documentTitle}
  description={copy.documentDescription}
  ogImage={assetUrl('/images/generated_bond_image.png')}
>
  <section data-scroll-section>
    <VideoCarousel lang={lang} videos={heroVideos} title="BOND" subtitle={copy.home.heroSubtitle}>
      <HeroNavbar slot="navbar" lang={lang} pathname={Astro.url.pathname} />
    </VideoCarousel>
  </section>

  <section data-scroll-section>
    <LookSection lang={lang} />
  </section>

  <section data-scroll-section>
    <SiteFooter lang={lang} />
  </section>
</Layout>

<script>
  // @ts-nocheck
  const sections = Array.from(document.querySelectorAll("[data-scroll-section]"));
  let scrollLocked = false;
  let wheelAccumulator = 0;

  function getClosestSectionIndex() {
    let closestIndex = 0;
    let minDistance = Number.POSITIVE_INFINITY;

    sections.forEach((section, index) => {
      const distance = Math.abs(section.getBoundingClientRect().top);
      if (distance < minDistance) {
        minDistance = distance;
        closestIndex = index;
      }
    });

    return closestIndex;
  }

  function scrollToSection(index) {
    const target = sections[index];
    if (!target) return;
    target.scrollIntoView({ behavior: "smooth", block: "start" });
  }

  window.addEventListener(
    "wheel",
    (e) => {
      if (sections.length < 2) return;

      if (scrollLocked) {
        e.preventDefault();
        wheelAccumulator = 0;
        return;
      }

      wheelAccumulator += e.deltaY;

      if (Math.abs(wheelAccumulator) < 45) return;

      const direction = wheelAccumulator > 0 ? 1 : -1;
      const currentIndex = getClosestSectionIndex();
      const nextIndex = Math.min(Math.max(currentIndex + direction, 0), sections.length - 1);

      wheelAccumulator = 0;

      if (nextIndex === currentIndex) return;

      e.preventDefault();
      scrollLocked = true;
      wheelAccumulator = 0;
      scrollToSection(nextIndex);

      window.setTimeout(() => {
        scrollLocked = false;
        wheelAccumulator = 0;
      }, 700);
    },
    { passive: false }
  );
</script>